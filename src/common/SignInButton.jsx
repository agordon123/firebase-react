import { Button } from "@mui/material";
import * as React from 'react';

function SignInButton(){

    return(
        <Button id="SignIn" variant="contained">Log in</Button>
    )
}

export default SignInButton;